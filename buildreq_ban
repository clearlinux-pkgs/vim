#
# This file contains build requirements that get picked up but are 
# undesirable. One entry per line, no whitespace.
#
# Autospec detects *.pro files in the tree and adds qtbase-dev as a dependency,
# but those files are not at all qmake related...
qtbase-dev
python-core
